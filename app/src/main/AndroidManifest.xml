<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.albertoguerpegui.task4_alberto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Login.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FormData.FormsActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Comunities.Comunity_Data" />
        <activity android:name=".Lessons.Class_Data" />
        <activity android:name=".Teachers.Teacher_Data" />
        <activity android:name=".General_Course.General_Course" />

    </application>

</manifest>